ng-repeat="data2 in myData | unique : 'categoryName' | myFilter: showItems track by $index"

<p>{{showItems = data2.featureCategory.categoryName}}</p>

  <tr ng-repeat="feature in myData | myFilter: showItems ">
  
  ng-show="loading && $index==inx1 && showItems == showingitems"
  
  ng-click="change(feature,$index,showItems)"
  
  
  
  
  ///in .js file
  
    $scope.change = function(obj, inx, showingitems) {
    $scope.showMessage = inx;
    $scope.showingitems = showingitems
